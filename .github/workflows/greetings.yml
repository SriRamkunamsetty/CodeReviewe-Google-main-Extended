name: Greetings
on: [pull_request_target, issues]
permissions:
  issues: write
  pull-requests: write
jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@753c925c8d1ac6fede23781875376600628d9b5d3 # v3.0.0
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        issue_message: "Welcome to CodeReviewer-Google! 👋 Thank you for opening your first issue. A maintainer or our AI agent will review it shortly."
        pr_message: "Welcome to CodeReviewer-Google! 🚀 Thank you for submitting your first Pull Request. Our CI pipelines are running, and a maintainer will review your code soon!"
